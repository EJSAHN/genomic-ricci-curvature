# Acquisition

The acquisition workflow reconciles BioProject metadata with an accession workbook, applies the documented two-row alias crosswalk, selects a population-balanced panel, and verifies FASTQ byte counts and MD5 values.
