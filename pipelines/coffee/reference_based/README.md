# Reference Based

Reference-derived features are built from independent read segments. Final performance estimates use nested cross-fitting; held-out rows are excluded from marker selection, PCA fitting, and scaling.
